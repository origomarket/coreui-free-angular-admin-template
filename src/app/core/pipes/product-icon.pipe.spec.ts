import { ProductIconPipe } from './product-icon.pipe';

describe('ProductIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductIconPipe();
    expect(pipe).toBeTruthy();
  });
});
